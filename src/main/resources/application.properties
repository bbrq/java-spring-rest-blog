spring.data.rest.default-page-size=8

#to add a version to the API itself 
spring.data.rest.base-uri=/api/v1 
#Now if we run the app, the API is located at 
#localhost:8080/api/v1/ (notice the /api/v1 at the end of the url).